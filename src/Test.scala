
import org.apache.spark._
import org.apache.spark.SparkContext

object Test {
  def main(args: Array[String]) = {
        println("Hello, world");
       // var sc=new SparkContext("local[*]","Rating Counter");
    }
  
}